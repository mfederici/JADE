architectures: MNIST
model:
  class: VariationalInformationBottleneck
  params:
    #################
    # Architectures #
    #################

    # Encoder
    encoder:
      class: 'Encoder'          # example in which we pass the class name as a parameter
      params:
        layers: [1024, 256]

    # Label classifier
    classifier:
      class: 'LabelClassifier'

    ##########################
    # Other model parameters #
    ##########################
    beta: 0.05

    # Number of latent dimensions
    z_dim: 64
