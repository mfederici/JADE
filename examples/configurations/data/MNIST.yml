train:
  class: MNISTWrapper
  params:
    root: $DATA_ROOT/datasets/MNIST
    split: train
    download: True

test:
  class: MNISTWrapper
  params:
    root: $DATA_ROOT/datasets/MNIST
    split: test
    download: True
