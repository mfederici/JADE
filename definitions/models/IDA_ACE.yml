class: IDAACETrainer
params:
  # Number of latent dimensions
  z_dim: 64

  #################
  # Architectures #
  #################

  # Encoder
  encoder:
    class: 'SimpleEncoder'
    params:
      dist: 'Delta'

  # Label classifier
  classifier:
    class: 'SimpleClassifier'

  # Environment Classifier
  env_classifier:
    class: 'SimpleConditionalEnvClassifier'

  ###########################
  # Optimization parameters #
  ###########################

  # Optimizer used for the encoder and the label classifier
  optim:
    class: 'RMSprop'
    params:
      lr: 0.0001

  # Adversarial optimizer used for training the environment classifier
  adv_optim:
    class: 'RMSprop'
    params:
      lr: 0.00005

  # Number of adversarial steps for each optimizer step
  n_adv_steps: 50

  # Size of the training batch
  batch_size: 256

  # Parameters for the beta-scheduler
  beta_scheduler:
    class: LinearScheduler
    params:
      start_value: 0.00001
      end_value: 0.99999
      n_iterations: 20000
      start_iteration: 20000
